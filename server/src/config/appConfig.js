const config = {
  port: 3001,
  clientOrigin: "http://localhost:5173",
  sessionSecret: "last-race-local-secret",
  databaseFile: "last-race.sqlite",
  sessionDatabaseFile: "sessions.sqlite",
  initialCoins: 20,
};

export default config;
